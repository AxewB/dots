alias se="sudoedit"
# alias nvim="bob run nightly"

# navigation
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# helix
alias hx='helix'

# clear
alias cl="clear"

# call fd and fzf to cd into directory
alias cddir='cd "$(fd --type directory | fzf)"'

alias n="nvim"
