#!/usr/bin/env bash

cd "$(dirname "$0")" || exit 1
source ./minsh-rofi-util

MENU="󰚰  Update|󰩹  NixOS generations"

CHOICE=$(generate_menu "$MENU" "System tools")

case "$CHOICE" in
"󰚰  Update") ./minsh-launch-tui ./minsh-nixos-update ;;
"󰩹  NixOS generations") ./minsh-launch-tui ./minsh-system-nixos-generations ;;#./minsh-rofi-system-clearnixosgens;;
esac
